fix: Restore "Add Column" button and resolve build errors

This commit includes two main fixes:

1.  Restored the "Add Column" button to the Kanban board.
    - The `AddColumnButton` component was re-added to `src/components/kanban/kanban-board.tsx` to restore the ability to add new columns directly from the board view.

2.  Resolved build errors related to page discovery.
    - Moved the authentication page from `src/app/(auth)/auth/page.tsx` to `src/app/auth/page.tsx`. This corrected a `PageNotFoundError` that was causing the `npm run build` command to fail.