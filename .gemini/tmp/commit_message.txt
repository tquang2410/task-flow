fix: Complete Project Settings UI and resolve build errors

Completed the implementation of the "Danger Zone" in the Project Settings
component, ensuring the delete project functionality is fully accessible.
Resolved several build errors and warnings encountered during development:
- Fixed unescaped apostrophes in `project-settings.tsx`.
- Re-added missing `useForm` import in `project-settings.tsx`.
- Removed unused `useRouter` import in `project-settings.tsx`.
- Adjusted layout classes in `project/[id]/page.tsx` for optimal rendering.
